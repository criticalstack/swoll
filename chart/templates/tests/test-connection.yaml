apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "swoll.fullname" . }}-test-connection"
  labels:
    {{- include "swoll.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "swoll.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
