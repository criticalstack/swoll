apiVersion: v1
kind: Service
metadata:
  name: {{ include "swoll.fullname" . }}
  labels:
    {{- include "swoll.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swoll.selectorLabels" . | nindent 4 }}
